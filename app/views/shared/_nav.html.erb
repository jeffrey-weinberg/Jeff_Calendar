<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="#">Jeff's App</a>
    <ul class="navbar-nav ml-md-auto">
      <li class="nav-item active">
        <a class="nav-item nav-link active" href="/">Home <span class="sr-only">(current)</span></a>
      </li>
      
      <% if user_signed_in? %>
        <li class="nav-item active">
          <a class="nav-item nav-link" href="/">Profile</a>
        </li>
        <li >
          <button type="button" class="btn btn-light" data-toggle="modal" data-target="#newEventModal">
            Add Event
          </button>
        </li>
        <li class="nav-item active">
            <%= link_to 'Logout', destroy_user_session_path, method: :delete, :class=>'btn btn-light' %>
        </li>
      <% else %>
        <li >
          <button type="button" class="btn btn-light" data-toggle="modal" data-target="#signInModal">
            Sign In
          </button>
        </li>
        <li class="nav-item active">
          <button type="button" class="btn btn-light" data-toggle="modal" data-target="#signUpModal">
            Sign Up
          </button>
        </li>
      <% end %>
    </ul>
  </nav>